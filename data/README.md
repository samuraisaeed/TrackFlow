# TrackFlow Local Data

TrackFlow keeps local project data in this folder when the local API server is running.

- `workspace.json` stores the active workspace.
- `backups/` stores JSON exports created from the Settings screen.
- `reports/` stores Markdown reports created from the report export buttons.

These files stay on this computer inside the TrackFlow project folder.
